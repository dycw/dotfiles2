# `dotfiles`

Dotfiles

## VM

```
./scripts/create-vm.sh
ssh -p 2222 root@127.0.0.1 'sh -s' < ./scripts/set-up-vm.sh
```

### Codex

```
sudo apt update
sudo apt install -y nodejs npm
sudo npm install -g @openai/codex
```
